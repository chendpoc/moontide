# Work Packet: Desktop Chat / Batch 6

- **Base:** `feat/desktop-ui` at `ac9a7d9`; worktree clean. Batch 5 UI is committed at `0e39330` and the Tauri icon set at `07b2180`.
- **Mode:** Implementation, split into two review units: 6A interaction/accessibility hardening, then 6B real-window visual QA.
- **Version goal:** close the remaining Desktop Chat v0.1 interaction, accessibility, responsive, and visual acceptance gates without adding product features.
- **Goal:** make keyboard/IME/focus/live-announcement behavior explicit and tested; make reduced-motion and White/Black contrast evidence reproducible; then validate the production UI in a real Tauri window against the four approved same-state references and record a final `design-qa.md` result.
- **Current hypothesis:** the Host, protocol, Controller, `RenderState`, and Batch 5 layout already contain the required business facts. Batch 6 should add only frontend-local interaction state and event-origin-aware presentation cues. A live approval announcement must be derived while reducing a live event, not inferred later by comparing snapshot-projected approvals in Svelte.
- **Source of truth:** repository `AGENTS.md`; `DESIGN.md`; `UI-V0.1-SCOPE.md` §6-9; `UI-INTERACTION.md` §6-14; `UI-VISUAL-DIRECTION.md` §11-14; `UI-V0.1-CHAT-IMPLEMENTATION-PLAN.md` Batch 6 and validation matrix; `docs/references/chat-ui/`; current frontend source and tests.
- **Confirmed decisions:** Host remains the owner of Session/Turn/tool/approval facts; Controller remains the owner of sequencing, resync, connection, and loaded Session identity; Svelte owns focus, shortcuts, announcement presentation, drawer state, theme, disclosure, clipboard feedback, and reading anchor only. The Session drawer remains docked and never becomes a modal layer. White and Black use identical geometry. Snapshot/history/replay must not generate live-only announcements.
- **Scope:** `frontend/src/lib/features/chat/`; focused `frontend/src/lib/projection/uiModel.ts` interaction reasons; `frontend/src/lib/projection/renderState.ts` to preserve live-event origin for an announcement; one focused `desktopController.ts` reducer-selection change that distinguishes direct delivery from snapshot replay without changing its public port or lifecycle behavior; keyboard constants; focused frontend tests; `frontend/styles.css`; Batch 6 QA documents and implementation captures. Rust runtime behavior, protocol schemas, persistence, and Tauri commands remain unchanged. A stale test-only Tauri icon assertion may be repaired if it blocks the required workspace gate.
- **Non-goals:** new message actions, attachments, model controls, sound, status bar, terminal/files/panels, multi-Session execution, redesigning the approved Batch 5 layout, pixel-level copying of reference brands, broad component-library refactors, or changing Session lifecycle semantics.
- **Risks:** key handlers can double-trigger buttons or fire during IME composition; focus restoration can steal focus from a user editing a new draft; Svelte-only approval comparison can announce snapshot/history state as if it were live; global motion suppression can hide meaningful state; nominal theme tokens may fail measured contrast in actual rendered combinations; real Tauri state setup may depend on local provider configuration.

## Review units

| Review | Tasks | Theme | Estimated diff |
|---|---|---|---|
| 6A | B6-1 through B6-6 | keyboard, IME, Session switch feedback, focus, announcements, reduced motion, contrast, focused tests | approximately 300-800 lines |
| 6B | B6-7 through B6-9 | real Tauri specimens, reference comparison, fixes, final QA evidence | approximately 150-700 lines plus captures; actual fixes depend on findings |

## Tasks

| Task | Deliverable | Acceptance |
|---|---|---|
| B6-1 Keyboard and IME | Composer Send shortcut, active-Turn Stop shortcut, Escape non-action, repeat/composition guards | one user intent produces one command; composition never submits; plain Enter remains a newline; Escape does not Stop or close the docked drawer |
| B6-2 Session transition feedback | candidate rows activate when allowed, expose a concrete block reason when switching is unsafe, and show an obvious inline spinner/text state while a switch is in flight | selected row remains non-actionable; running/approval/connection/resync states explain why other rows cannot switch; the target row and Recent list expose `Loading…`; reduced motion retains text; keyboard activation matches pointer activation |
| B6-3 Focus lifecycle | deterministic focus after New Chat and Session load, stable focus during stream/notice/theme updates | Blank focuses the Composer; Loaded focuses a meaningful conversation heading/region after an explicit switch; background updates never steal focus |
| B6-4 Live announcements | polite status for explicit theme/connection/copy changes and exactly-once live approval notification | initial snapshot/history/replay and stream tokens are silent; live approval is announced once without exposing tool input or secrets |
| B6-5 Motion and contrast | reduced-motion rules and measured White/Black token/component contrast evidence | nonessential transitions/spinners stop under reduced motion; normal text is at least 4.5:1 and boundaries/focus/non-text state at least 3:1 |
| B6-6 6A validation | focused component/App/projection tests plus frontend test/check/build | all 6A behavior is reproducible; no Controller/protocol ownership change; no unresolved P0/P1/P2 before review |
| B6-7 Real Tauri smoke | Blank/Loaded and White/Black passes at `1440x900`, `1280x800`, `960x720`, and `1440x900` at 200% zoom | no critical action is clipped; drawer resize/collapse, focus, first-send, and Session switch work; WebView console has no error |
| B6-8 Same-state visual comparison | implementation captures compared with the four files in `docs/references/chat-ui/` | Blank and Loaded comparison uses only the approved density, hierarchy, reading-width, message distinction, sticky Composer, and selection criteria |
| B6-9 Final QA | fix all P0/P1/P2; write `docs/design-qa.md`; run final frontend/workspace checks and independent Standards/Spec review | `design-qa.md` says `passed`, evidence and unknowns are recorded, and user visual review is complete before commit |

- **Agent task:** implement and validate review 6A only after the split below is confirmed; stop for review and commit authorization before starting 6B.
- **Reviewer:** independent Tidewatch reads this packet, canonical docs, live source, full tracked/untracked diff, and validation evidence after each review unit.
- **User parallel task for 6A:** completed as baseline feedback. Chinese input works; `Control+.` has no Stop effect while idle and inserts a period; Session candidates appear non-clickable; the Tab path and focus indicator are not discoverable. These observations become explicit 6A tests; active-Turn Stop and the exact Session block condition still require controlled reproduction.
- **Shared acceptance for 6A:** keyboard and IME behavior matches `UI-INTERACTION.md`; explicit navigation has a useful focus destination; background state never steals focus; only live events create live-only announcements; White/Black remain geometrically identical; reduced motion preserves understandable text/shape state; frontend tests/check/build pass; ownership remains unchanged.
- **Shared acceptance for 6B:** all required viewport/theme specimens and same-state comparisons are recorded; real Tauri first-send/Session switch/drawer/focus paths pass without console errors; every P0/P1/P2 is fixed; final `just check`, independent review, and user visual review are recorded.
- **Evidence:** clean baseline at `ac9a7d9`; `pnpm test` passes 78/78 tests and `pnpm run check` reports 0 errors/0 warnings. Existing code already guards `KeyboardEvent.isComposing`, implements drawer Arrow/Home/End resizing, provides focus styles, persists White/Black, exposes copy feedback, and unit-tests one allowed Session candidate click. User baseline testing found that the real blocked/allowed Session condition and Tab visibility are not understandable. Missing evidence includes the active-Turn Stop shortcut, blocked Session explanation, complete focus lifecycle, event-origin-safe live approval announcement, measured contrast, four viewport/zoom specimens, and real Tauri console smoke.
- **Open questions:** real provider/runtime availability for 6B will be checked before stateful smoke. If live approval origin cannot be preserved inside the existing frontend projection without changing Controller or protocol contracts, stop and return to architecture alignment rather than announcing snapshot state incorrectly.
- **Decision changes:** Batch 6 is split into 6A and 6B so interaction correctness is reviewed before visual evidence and finding-driven fixes. This changes delivery order, not product behavior or ownership. User review replaced the internal-sounding Session-switch copy with the visible `Loading…` state. Independent review found that direct events and snapshot replay must select different reducer entry points, so the Controller now uses `reduceLiveEnvelope` only for direct delivery while replay remains on `reduceEnvelope`; its public contract and lifecycle ownership are unchanged. The required workspace gate exposed a stale `shell.rs` assertion left by the committed multi-platform logo configuration; 6A updates only that test to verify the five required platform icons and does not change runtime behavior.
- **Next smallest experiment:** add failing focused tests for Composer composition/repeat handling, active-Turn Stop, allowed/blocked Session activation, explicit navigation focus, and live-only announcement origin before modifying behavior.
- **Stop conditions:** any need to change Host/Controller/protocol ownership or public contract; any proposal to infer a business fact from DOM timing; any real-window requirement that depends on unavailable credentials or external state; any visual fix that changes the approved layout/product scope; or any need to expose secret/tool input in accessible text.

## Review unit 6C: Desktop storage root correction

- **Base:** `feat/desktop-ui` with the uncommitted Batch 6/7 work preserved. This corrective unit may touch only Desktop storage/bootstrap documentation and Rust paths; it must not rewrite unrelated frontend or Session-history changes.
- **Mode:** Replan L2, confirmed by the user on 2026-09-02. The persisted Desktop settings location and project-root ownership change; CLI behavior and protocol contracts do not.
- **Goal:** make Desktop settings, Session Item Logs, and Agent Event Logs independent of the Tauri process working directory, while keeping the Agent workspace root explicit.
- **Confirmed decisions:** Desktop settings live at Tauri `app_config_dir()/settings.json`; Desktop content lives at `app_data_dir()/content/{sessions,runs}`; settings version 3 stores an explicit absolute `project_root`, provider, model, and base URL; provider credentials remain environment-owned and are never read from Desktop JSON. Inherited process environment values override the optional, explicit `project_root/.env` development fallback; process-cwd dotenv discovery is forbidden. Development and packaged builds use the same Tauri path rules for their configured bundle identifier; the release must adopt a production identifier before user data exists.
- **Scope:** `DESIGN.md`; this Work Packet; `src-tauri/src/{bootstrap,shell,lib}.rs`; focused Desktop bootstrap/storage tests; one-time local development-data copy into the resolved Application Support directory.
- **Non-goals:** changing CLI settings lookup; adding a project picker, keychain, migration framework, database, protocol field, frontend setting UI, or automatic deletion of legacy `.moontide` directories.
- **Risks:** startup must not construct the runtime before a Tauri path resolver exists; a missing/corrupt version-3 settings file or malformed explicit project dotenv must fail explicitly; legacy development Session files must remain recoverable; Agent tools must use `project_root`, never the content directory, as `cwd`.
- **Agent task:** introduce a narrow Desktop storage layout, initialize the runtime inside Tauri setup, parse version-3 settings from the stable application directory, route Session/Run storage to `content/`, copy current development data without deleting its source, and validate a real Agnes Turn from the resolved content root.
- **Reviewer:** Tidewatch checks the live source, dirty diff, path ownership, credential handling, focused tests, and workspace validation independently after implementation.
- **User parallel task:** completed as a Decision task. The user selected fixed Desktop application storage rather than process-`cwd` storage and confirmed that development mode must follow the Desktop layout.
- **Shared acceptance:** changing the shell launch directory cannot change the selected Provider or Session catalog; Desktop JSON contains no API key; `AgentConfig.cwd` equals the configured project root; sessions/runs are under Tauri Application Support content; legacy development data is copied but not deleted; focused tests and `just check` pass; no CLI/protocol/frontend behavior changes.
- **Next smallest experiment:** add a pure storage-layout test and a bootstrap test with separate application and project temporary directories, then observe the old `ProjectPaths::resolve(current_dir)` implementation fail the expected path assertions before replacing it.
- **Stop conditions:** any need to infer a packaged-app project root, silently select an arbitrary workspace, overwrite/delete legacy content, place credentials in JSON, or change a public protocol/runtime ownership boundary.
