/** Global composer shortcuts; handlers live in feature/composer or app shell. */
export const COMPOSER_SUBMIT_KEY = "Enter" as const;
export const COMPOSER_SUBMIT_MODIFIERS = ["metaKey", "ctrlKey"] as const;
export const COMPOSER_STOP_KEY = "." as const;
export const COMPOSER_CANCEL_KEY = "Escape" as const;
