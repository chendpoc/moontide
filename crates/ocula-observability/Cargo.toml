[package]
name = "ocula-observability"
description = "Ocula REPL observability modes and stderr trace"
version.workspace = true
edition.workspace = true

[dependencies]
owo-colors = { workspace = true }
