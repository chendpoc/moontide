[package]
name = "ocula-protocol"
description = "Ocula LLM protocol and Session log types"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
