{
  "name": "moontide-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "MoonTide monorepo workspace",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm build:core && pnpm --filter moontide run build",
    "start": "pnpm --filter moontide run start",
    "dev": "pnpm --filter moontide run dev",
    "ping": "tsx packages/llm/src/healthcheck/ping.ts",
    "test": "vitest run",
    "test:conformance": "vitest run tests/conformance",
    "eval": "tsx --tsconfig tsconfig.dev.json packages/evals/scripts/run-evals.ts",
    "eval:test": "vitest run --config packages/evals/vitest.config.ts tests",
    "eval:integration": "vitest run --config packages/evals/vitest.config.ts examples",
    "typecheck": "pnpm typecheck:core && pnpm --filter moontide run typecheck",
    "check": "pnpm lint && pnpm typecheck && pnpm test && pnpm eval:test",
    "eval:pr": "pnpm eval:test",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "prepare": "husky",
    "dev:ui": "cargo run --manifest-path ui/Cargo.toml -- --workdir .",
    "build:core": "pnpm -r --filter '@moontide/*' run build",
    "typecheck:core": "pnpm -r --filter '@moontide/*' run typecheck",
    "test:core": "vitest run packages/agent-core/tests"
  },
  "lint-staged": {
    "*.{ts,tsx}": "eslint --fix"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.13.10",
    "eslint": "^10.8.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.2.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.65.0",
    "vitest": "^3.0.9"
  }
}
