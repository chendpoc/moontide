# Tool hints (dev collection)

When `OCULA_DEV_TOOL_LEARNING=1`, the agent can call `record_tool_hint` to capture suboptimal tool usage patterns.

Each hint is written under this directory as `{date}-{tool}-{slug}.md` for human review before upstreaming improvements via GitHub PR.

**This directory is not auto-applied to tool source** — hints are proposals only.
