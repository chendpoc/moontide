/** Public product branding and technical slug (single source of truth). */
export const PRODUCT_NAME = "MoonTide" as const;
export const COMPANY_NAME = "OceanSpark" as const;
/** Lowercase slug for data dirs, env prefix, crate names. */
export const PRODUCT_SLUG = "moontide" as const;
