# @moontide/agent-common

Shared agent protocol (RunEvent, RunConfig, Effect ports). See [AGENTS.md](../../AGENTS.md) §7.2.

- `protocol/` — common contract types
- `PROTOCOL_VERSION` — breaking changes bump this integer
