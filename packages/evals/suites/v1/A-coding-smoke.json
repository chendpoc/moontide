{
  "version": "2",
  "bucket": "A",
  "cases": [
    {
      "id": "coding-read-export",
      "category": "coding",
      "gradingMode": "objective",
      "steps": [{ "type": "prompt", "content": "Read sample.ts. Does it export a function named runLoop? Answer yes or no with the file name." }],
      "setup": { "files": { "sample.ts": "export function runLoop() { return 'ok'; }\n" } },
      "expectedChecks": [
        { "kind": "reply_contains", "value": "runLoop" },
        { "kind": "reply_matches", "pattern": "(?i)\\byes\\b" }
      ]
    },
    {
      "id": "coding-no-default-export",
      "category": "coding",
      "gradingMode": "objective",
      "steps": [{ "type": "prompt", "content": "Read utils.ts. Does it have a default export? Answer yes or no only." }],
      "setup": {
        "files": {
          "utils.ts": "export function add(a: number, b: number) { return a + b; }\nexport function sub(a: number, b: number) { return a - b; }\n"
        }
      },
      "expectedChecks": [{ "kind": "reply_matches", "pattern": "(?i)\\bno\\b" }]
    },
    {
      "id": "coding-read-return-literal",
      "category": "coding",
      "gradingMode": "objective",
      "steps": [{ "type": "prompt", "content": "Read greet.ts. What string literal does greet() return?" }],
      "setup": { "files": { "greet.ts": "export function greet() { return 'hello'; }\n" } },
      "expectedChecks": [{ "kind": "reply_contains", "value": "hello" }]
    },
    {
      "id": "coding-spot-off-by-one",
      "category": "coding",
      "gradingMode": "objective",
      "steps": [{ "type": "prompt", "content": "Read sum.ts. The loop has an off-by-one error. In one sentence, say what is wrong." }],
      "setup": {
        "files": {
          "sum.ts": "export function sum(arr: number[]) {\n  let total = 0;\n  for (let i = 0; i <= arr.length; i++) {\n    total += arr[i];\n  }\n  return total;\n}\n"
        }
      },
      "expectedChecks": [
        { "kind": "reply_matches", "pattern": "(?i)(<=|length|off.by.one|out of bounds|undefined)" }
      ]
    },
    {
      "id": "coding-read-config-default",
      "category": "coding",
      "gradingMode": "objective",
      "steps": [{ "type": "prompt", "content": "Read config.ts. What is the default value of PORT?" }],
      "setup": {
        "files": {
          "config.ts": "export const PORT = Number(process.env.PORT ?? 3000);\n"
        }
      },
      "expectedChecks": [{ "kind": "reply_contains", "value": "3000" }]
    },
    {
      "id": "coding-count-named-exports",
      "category": "coding",
      "gradingMode": "objective",
      "steps": [{ "type": "prompt", "content": "Read api.ts. How many named exports does it define? Reply with the number only." }],
      "setup": {
        "files": {
          "api.ts": "export function getUser() { return 'u'; }\nexport function saveUser() { return 'ok'; }\nexport const API_VERSION = 1;\n"
        }
      },
      "expectedChecks": [{ "kind": "reply_matches", "pattern": "\\b3\\b" }]
    },
    {
      "id": "coding-read-first-test-name",
      "category": "coding",
      "gradingMode": "objective",
      "steps": [{ "type": "prompt", "content": "Read math.test.ts. What is the name of the first test (it(...))?" }],
      "setup": {
        "files": {
          "math.test.ts": "import { describe, it, expect } from 'vitest';\nimport { add } from './math';\n\ndescribe('math', () => {\n  it('adds two numbers', () => {\n    expect(add(1, 2)).toBe(3);\n  });\n});\n",
          "math.ts": "export function add(a: number, b: number) { return a + b; }\n"
        }
      },
      "expectedChecks": [{ "kind": "reply_contains", "value": "adds two numbers" }]
    },
    {
      "id": "coding-package-module-type",
      "category": "coding",
      "gradingMode": "objective",
      "steps": [{ "type": "prompt", "content": "Read package.json. Is type module or commonjs?" }],
      "setup": {
        "files": {
          "package.json": "{\n  \"name\": \"demo\",\n  \"type\": \"module\",\n  \"version\": \"1.0.0\"\n}\n"
        }
      },
      "expectedChecks": [{ "kind": "reply_contains", "value": "module" }]
    },
    {
      "id": "coding-null-guard-missing",
      "category": "coding",
      "gradingMode": "objective",
      "steps": [{ "type": "prompt", "content": "Read fetchUser.ts. If input id is empty, what does the function return?" }],
      "setup": {
        "files": {
          "fetchUser.ts": "export function fetchUser(id: string) {\n  if (!id) return null;\n  return { id, name: 'demo' };\n}\n"
        }
      },
      "expectedChecks": [{ "kind": "reply_contains", "value": "null" }]
    },
    {
      "id": "coding-read-interface-field",
      "category": "coding",
      "gradingMode": "objective",
      "steps": [{ "type": "prompt", "content": "Read types.ts. What is the type of User.id?" }],
      "setup": {
        "files": {
          "types.ts": "export interface User {\n  id: string;\n  email: string;\n}\n"
        }
      },
      "expectedChecks": [{ "kind": "reply_contains", "value": "string" }]
    }
  ]
}
